name: chapter10
description: Flutter plugins.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 相机
  camera: ^0.5.4
  path_provider: ^1.3.0
  path: ^1.6.2
  # 视频
  video_player: ^0.10.2


  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

