name: chapter09
description: Flutter 数据持久化 sp

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  # 数据持久化sp
  shared_preferences: ^0.5.3
  # 文件路径 读写
  path_provider: ^1.3.0
  # sqflite数据库
  sqflite: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

